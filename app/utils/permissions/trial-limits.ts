/** Dias de trial quando `TRIAL_LIMITS_DISABLED` estiver `false`. */
export const TRIAL_DAYS = 14

/**
 * Quando `true`, não expira trial nem bloqueia o painel por falta de assinatura.
 * Volte para `false` quando quiser cobrar / limitar de novo.
 */
export const TRIAL_LIMITS_DISABLED = true